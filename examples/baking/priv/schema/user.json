{
  "asyncapi": "3.0.0",
  "info": {
    "title": "Test API",
    "version": "0.0.1",
    "description": "A service that uses timers"
  },
  "defaultContentType": "application/json",
  "servers": {
    "production": {
      "host": "127.0.0.1",
      "protocol": "mqtt",
      "description": "Test broker",
      "variables": {
        "port": {
          "description": "MQTT port",
          "default": "1883",
          "enum": [
            "1883"
          ]
        }
      }
    }
  },
  "operations": {
    "start_baking": {
      "action": "send",
      "channel": {
        "$ref": "#/channels/start_baking"
      }
    },
    "baking_done": {
      "action": "receive",
      "channel": {
        "$ref": "#/channels/baking_done"
      }
    },
    "baking_not_done": {
      "action": "receive",
      "channel": {
        "$ref": "#/channels/baking_not_done"
      }
    }
  }
}