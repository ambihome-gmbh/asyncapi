defmodule MultiStackTest do
  use ExUnit.Case
  require Asyncapi.TestHelper

  alias KimSchema.MessagePayload, as: P

  @broker Application.compile_env(:asyncapi, :broker)

  setup do
    Asyncapi.TestHelper.start_broker()
    :ok
  end

  Asyncapi.TestHelper.generate_tests(KimService, TestUserSchema, @broker)

end
