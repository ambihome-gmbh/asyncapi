{
    "asyncapi": "3.0.0",
    "info": {
        "title": "CEMI API — Server",
        "version": "0.1",
        "description": "Receives l_data_req; sends l_data_ind (incl. group_resp)."
    },
    "defaultContentType": "application/json",
    "operations": {
        "req": {
            "summary": "Receive user requests (l_data_req; apci in {group_read, group_write})",
            "action": "receive",
            "channel": {
                "$ref": "#/channels/CEMI_REQ"
            }
        },
        "ind": {
            "summary": "Send indications (l_data_ind; apci in {group_read, group_write, group_resp})",
            "action": "send",
            "channel": {
                "$ref": "#/channels/CEMI_IND"
            }
        }
    }
}